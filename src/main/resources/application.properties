# ============================================
# AUTH0 CONFIGURATION (NATIVE SPRING SECURITY)
# ============================================

# Auth0 Issuer (deine Domain)
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://dev-o8qajefgy1cmtw7t.us.auth0.com/

# Auth0 Audience (deine API Identifier)
spring.security.oauth2.resourceserver.jwt.audiences=https://puppyracer.api

# ============================================
# DATABASE CONFIGURATION (FALLBACK - WIRD DURCH ENV OVERRIDEN)
# ============================================

# WICHTIG: KEINE feste DB hier - wird durch Environment Variables überschrieben!
# spring.datasource.url=  # LEER LASSEN oder kommentieren

# ============================================
# JPA / HIBERNATE CONFIGURATION
# ============================================

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false  # In Prod false
spring.jpa.properties.hibernate.format_sql=false

# ============================================
# SERVER CONFIGURATION
# ============================================

server.port=8081
server.servlet.context-path=/

# ============================================
# PROFILE CONFIGURATION
# ============================================

# Activate profiles based on environment
spring.profiles.active=@activatedProperties@